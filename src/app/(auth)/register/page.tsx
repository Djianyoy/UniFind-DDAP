import RegisterContainer from '@/feature/(auth)/register/RegisterContainer'
import React from 'react'

const page = () => {
  return (
    <section>
      <RegisterContainer/>
    </section>
  )
}

export default page