import LoginContainer from '@/feature/(auth)/login/LoginContainer'
import React from 'react'

const page = () => {
  return (
    <section>
      <LoginContainer/>
    </section>
  )
}

export default page